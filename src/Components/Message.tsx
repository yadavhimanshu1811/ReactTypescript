function Message() { // Function based component;
    const name = "Himanshu";
    if(1){
        return (
            <h1>Hello {name}</h1>
        )
    }else return null;
}

export default Message;